# Release Matrix

Date: 2026-05-07

## Targets

- Production Web URL: https://minougun.github.io/jijii-kobushi/
- GitHub repo: https://github.com/minougun/jijii-kobushi
- Local repo: `/mnt/c/Users/minou/jii-kobushi`

## Purpose

This file records which Web original build is being reviewed and which Switch-port artifacts derive from it. It exists because commercial QA needs a verifiable build identity, not only a human-written commit note.

## Current Lineage

| Area | Commit / Artifact | Notes |
|---|---|---|
| Web QA hardening | `b02a1e8 Harden web QA follow-up` | Storage failure, popout focus, smoke hardening. |
| Final Web UI fixes | `7b55142 Fix final UI review findings` | Font subset, help guide ARIA, language selector semantics, small-landscape difficulty width. |
| Switch asset application | `2b20eee Update Switch asset manifest for UI font fix` | Switch runtime asset manifest points at the regenerated Web font object. |
| Production build metadata | `version.json` | Generated by GitHub Actions from `GITHUB_SHA` during Pages deployment. |

## Release Gate

Before claiming a production build is current:

```bash
npm run check:local-100
git push origin main
npm run test:web-smoke:pages
```

`npm run test:web-smoke:pages` now fetches `https://minougun.github.io/jijii-kobushi/version.json` and verifies the deployed commit matches the local `HEAD` by default.

`npm run check:local-100` is the local completion gate for this repository. It runs the Web commercial completion gate, captures the commercial review evidence screenshots under `docs/commercial-review-evidence-2026-05-07/`, then runs the Switch local completion gate including Unity tests, parity validation, Windows prototype build, and player smoke checks.

## Commercial Persona Score Gate

`npm run review:persona` is a regression signal, not a commercial-quality score. A defensible commercial score of 95+ requires direct evidence for these screens and flows:

| Flow | Desktop 1280x720 | Mobile portrait 390x844 | Mobile landscape 844x390 | Notes |
|---|---|---|---|---|
| OP / title | Required | Required | Required | Artwork, title hierarchy, difficulty buttons. |
| Help / settings | Required | Required | Required | Discoverability, focus, readable text. |
| Battle TAP/HOLD/MASH | Required | Optional | Required | Gold line clarity, input feedback, mobile tap button. |
| Pause popouts | Required | Optional | Required | Input offset/help/settings focus return. |
| Result screen | Required | Optional | Required | Commercial hierarchy and no duplicate UI. |
| ED bonus | Required | Optional | Required | Smoothness and UI consistency. |
| Loop 2 doodle | Required | Optional | Required | Intentional joke, not broken UI; enemy HP readable. |

## Open Commercial Review Risks

- Real mobile device feel still needs human verification for iOS Safari and Android Chrome.
- Screen reader real-read verification still needs human verification with VoiceOver/NVDA.
- Commercial 95+ should not be claimed from the automated persona score alone.
